ducolam.com www.ducolam.com {
  root * /usr/share/caddy
  file_server
  try_files {path} /index.html

  reverse_proxy /api/* backend:8000

  tls /home/admin/caddy-certs/cert.pem /home/admin/caddy-certs/private-key.pem
}
